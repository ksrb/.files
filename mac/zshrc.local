# General {
    # Add node_modules/.bin to path
    export PATH=./node_modules/.bin:$PATH

    # Disable ctrl-s and ctrl-q http://unix.stackexchange.com/a/72092
    stty -ixon

    # Directory shortcuts
    prog (){ cd ~/Documents/Programming/$1 }
    compctl -W $HOME/Documents/Programming/ -/ prog
# }
